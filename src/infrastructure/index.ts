/**
 * Infrastructure Layer
 * 
 * This layer contains implementations of the interfaces defined in the core layer.
 * It handles all external concerns like databases, caches, and message brokers.
 * 
 * Structure:
 * - database/    : Prisma client, database repositories
 * - cache/       : Redis client and cache implementations
 * - messaging/   : RabbitMQ client and message handlers
 * - external/    : Third-party API integrations
 */

export {};
