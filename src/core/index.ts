/**
 * Core Domain Layer
 * 
 * This layer contains the heart of the business logic.
 * It is completely framework-agnostic and should have no dependencies
 * on external libraries or infrastructure concerns.
 * 
 * Structure:
 * - entities/     : Business entities (e.g., Event, Ticket, User)
 * - value-objects/: Immutable objects representing concepts (e.g., Money, Email)
 * - services/     : Domain services containing business logic
 * - repositories/ : Repository interfaces (implementations in infrastructure)
 * - errors/       : Domain-specific error types
 */

export {};
